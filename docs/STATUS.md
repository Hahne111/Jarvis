# JARVIS – STATUS

Current version: 0.5.0.dev0 (Phase 6 abgeschlossen bis auf Tauri-Wrapper; Phase 7 läuft)
Current phase: PHASE 7 Live Coding Mode – Schritte 43 (Projektbaum/Dateiansicht, noch ohne Monaco), 44 (Diff-Events + Panel), 45 (Terminal-Panel), 46 (Sandboxed Runner), 47 (Preview) umgesetzt; offen 48 Writer/Test/Verifier-Rollen an `workspace.*` binden (Coding-Workflow im Agenten), 49 Artifact Registry, Monaco-Editor (erst mit Tauri-Bundle, kein CDN). Phase 6 Rest: Tauri-Wrapper. Vorher (Phase 6): Exit „App öffnen und verifizieren“ ✔ (FakeDesktop), „Screen Context optional an Core“ ✔ (`computer.screenshot`, Datei lokal), „HUD bleibt flüssig während Agent arbeitet“: Architektur (rAF-Batching, kein Polling) steht, Messung mit echtem Agent-Lauf offen. Offen: Tauri-Wrapper (natives Fenster/Autostart) sobald Toolchain lokal vorhanden; echter Desktop-Lauf mit `JARVIS_DESKTOP=prototype`. Phase-6-Exit „App öffnen und verifizieren“ mit FakeDesktop erfüllt; „HUD bleibt flüssig“ erst mit der Shell messbar.
Last completed milestone: „apps/desktop: coding mode panels and sandboxed preview“ – API: `GET /workspace/{mission}/files|file|diff` (read-only Sichten über den `WorkspaceManager`, gleiche Pfad-Sandbox) und `GET /workspace/{mission}/preview/<path>` (statische Auslieferung nur aus dem Missions-Workspace, Content-Type-Whitelist, `Content-Security-Policy: sandbox allow-scripts`, `nosniff`, `no-store`, Verzeichnis → index.html, Escape ⇒ 404). HUD: Panel „CODING“ mit Missionswahl, Projektbaum, Dateiansicht (`<pre>`; Monaco bewusst nicht per CDN), Diff-Panel (farbig, gespeist aus `workspace.file.changed` bzw. `/diff`), Terminal-Panel (Chunks aus `workspace.run.output`, stderr rot, Exit-Zeile aus `workspace.run.finished`), Preview-`<iframe sandbox="allow-scripts">`. Headless in Chromium verifiziert (Baum, Datei, Preview-Text sichtbar, keine JS-Fehler). 1 Test (172 Core gesamt).
Previously: „adapters/workspace: sandboxed project workspaces and runner“ – `adapters/workspace/manager.py`: `WorkspaceManager` (Root aus `JARVIS_WORKSPACE_ROOT`, ein Verzeichnis pro Mission = `correlation_id` des laufenden Aufrufs, nie wählbar durch den Agenten; `resolve()` blockt `..`, absolute Pfade, `~`, Escape per Symlink und den verwalteten `.jarvis`-Ordner; Schreiben legt Versionskopien an; `diff` gegen letzte Version; `run` nur Allowlist-Kommandos (Argumente ohne absolute Pfade/`..`), Timeout mit Kill, `RLIMIT_CPU`, bereinigte Umgebung (kein API-Key, HOME=Workspace), Ausgabe gestreamt und gedeckelt). `capabilities.py`: `workspace.list|read|diff` (P0), `workspace.write` (P2, Verifier `workspace.file_matches` per SHA-256), `workspace.run` (P3 ⇒ Approval, Verifier `workspace.run_exit_code`: Exit 0 = Ziel erreicht, Timeout/≠0 = nicht erreicht); Events `workspace.file.changed` (Diff, Actor) und `workspace.run.started|output(chunked)|finished`. Immer registriert (Sandbox), `jarvis/data/` ist gitignored. 4 Tests (171 Core gesamt): alle Escape-Varianten blockiert, Versionen/Diffs, Allowlist/Timeout/Env-Scrub, kompletter Pfad durch Gateway inkl. Approval und „Programm schlägt fehl ⇒ Tool lief, Ziel nicht erreicht“.
Previously: „apps/desktop: web-first HUD shell“ – ADR-0003 (Web-first vor Tauri, Begründung Toolchain/CI), `apps/desktop/web/` (index.html, hud.css mit Design-Tokens Amber/Cyan und zustandsgetriebener Core-Animation für idle/listening/thinking/speaking/working/awaiting_approval/halted, hud.js als ES-Modul: WS-Replay-then-live ab letztem `seq` mit Reconnect, Event-Batching pro Animationsframe, Bereichsfilter laut HUD_EVENTS.md, Approval-Karten mit Confirm/Passkey/Deny, Missions, Memory-Panel mit pin/correct/forget, Kill Switch/Resume, Latenz-p95-Kacheln aus `telemetry.latency`, Kommandozeile → `/commands`). Core liefert unter `/hud/` aus, `/` leitet um. Verifiziert headless in Chromium (Playwright): Kommandos „open vscode“ und „Lautstärke auf 20“ durchgelaufen, Kill Switch → Core-Anzeige HALTED, Resume → idle, 24 Rail-Events, keine JS-Fehler; Screenshot lokal geprüft. `core.__version__` auf 0.4.0.dev0. 1 zusätzlicher API-Test (167 Core gesamt).
Previously: „core: presence states and HUD event contract“ – `core/presence.py`: `PresenceService` abonniert den Bus und leitet pro Gerät (`device_id`, sonst `core`) deterministisch idle/listening/thinking/speaking/working/awaiting_approval ab (voice.* → Sprachzustände, agent.run.* → working, permission.ask/approved/denied → awaiting_approval und zurück, pausierte Runs bleiben aktiv), `gateway.halted` überlagert alle Geräte mit halted (urgent) bis `gateway.resumed`; publiziert nur echte Änderungen als `presence.changed`, `rebuild()` aus dem Log ohne neue Events (Recover). `GET /presence`, Snapshot in `/health`. `docs/HUD_EVENTS.md`: Transport (Replay + WS lückenlos), Envelope, Zuordnung Event-Typen → HUD-Bereiche (Core, Voice, Mission, Agent Rail, Tool-Log, Approval, Kill Switch, Memory, Performance), Shell-Regeln. 3 Tests (166 Core gesamt).
Previously: „adapters: desktop capabilities behind the core“ – `adapters/desktop/`: `DesktopBackend`-Protokoll, `FakeDesktop` (In-Memory-Modell inkl. „lügender“ Launcher für Verifier-Tests) und `PrototypeDesktop` (lazy Wrapper über `jarvis/tools/app_control|desktop|system`, blockierende Aufrufe in Worker-Threads), Manifeste: `computer.open_app` (P1, requires device.trusted, Verifier `computer.process_running` – bei PrototypeDesktop über optionales psutil, sonst UNKNOWN), `computer.list_windows` (P0), `computer.focus_window` (P2, Verifier `computer.window_focused`), `computer.screenshot` (P0, Datei bleibt lokal), `computer.type_text`/`computer.press_key` (P3 ⇒ Approval, irreversibel, Verifier `computer.input_delivered`), `system.info` (P0), `system.set_volume` (P1, Verifier `system.volume_is`), `system.lock_screen` (P2, Verifier `system.locked`). Verifier-Regel: kein unabhängiges Signal ⇒ UNKNOWN, nie Erfolg. CoreRuntime: `JARVIS_DESKTOP=off|fake|prototype`; Intent Router: Fast-Path-Regeln für App öffnen, Sperren, Lautstärke, Fensterliste (freier Text wie „type hello“ bleibt Agent-Pfad). 6 Tests (163 Core gesamt).
Previously: „voice: pipeline interfaces and core bridge“ – `voice/`: `interfaces.py` (WakeWordDetector, SpeechToText mit Partial/Final-Transkripten, TextToSpeech mit `stop()`, TurnDetector + `SilenceTurnDetector` mit Satzende-Heuristik), `session.py` (`VoiceSession` idle → wake_ack → listening → thinking → speaking → follow_up mit `voice.*`-Events; `VoiceBridge`: Wake-Ack-Event *vor* jeder Core-Arbeit, Transkript → `run_text_command` (dieselbe Pipeline wie `/commands`, jetzt in `core/api/commands.py`), phrase-level TTS-Streaming, Barge-in stoppt Audio sofort und parkt die Session, Stop-Phrase zieht zusätzlich den Kill Switch, Follow-up-Modus, neue Wake während Sprechen = Unterbrechung + neuer Turn), `telemetry.py` (`telemetry.latency`-Events mit Budget-Flag, p50/p95 nearest-rank, Summary aus dem Log), `personality.py` (No-Filler, Satzlimit, „Done.“, Ernst-Erkennung, Phrasen-Splitter), `fakes.py` (loop-sichere Fakes), `adapters/prototype.py` (Wake/STT/TTS des Prototyps in Worker-Threads, `jarvis/` unverändert), `python -m voice` (+ `JARVIS_VOICE_FAKE=1`). 9 Tests (157 Core gesamt): Wake-Ack vor `mission.created`, Barge-in gemessen < 150 ms, Stop-Phrase ⇒ `gateway.halted`, Fast Path ohne Provider, Follow-up, Telemetrie-Budgets.
Previously: „core: context builder, memory capabilities and What-JARVIS-Knows API“ – `core/memory/context.py`: `ContextBuilder.build(goal, project_scope, cloud, max_items, max_chars)` wählt per Store-Suche relevante Items, schließt `secret` bei Cloud-Modellen aus, rendert `[type, confidence, source] subject predicate: value` als Daten-Block (nicht als Instruktion); Coordinator hängt ihn pro Run und Sub-Run an den Systemprompt und emittiert `memory.context_used` mit den memory_ids. `core/memory/capabilities.py`: `memory.recall` (P0, nie secret), `memory.remember` (P1, Verifier `memory.stored`), `memory.correct`/`memory.forget` (P3 ⇒ Approval, Verifier `memory.corrected`/`memory.gone`) – der Agent lernt nur durch das Gate; Gateway stellt `current_correlation_id`/`current_actor` als ContextVar bereit, damit Memory-Events zur Mission korrelieren. API: `GET /memory?q&type&project`, `GET /memory/{id}`, `POST /memory/{id}/correct|forget|pin|unpin|temporary`, `POST /memory/forget_since`, `GET|POST /memory/policy`, `POST /memory/dont_learn`; Debug-Dashboard-Panel „What JARVIS Knows“. Suche: Embedder-Ähnlichkeit zählt erst ab 0.5 (Hash-Kollisionen erzeugten Scheintreffer). 6 Tests (148 Core gesamt); Test „Korrektur über API ändert den Agent-Kontext“ grün.
Previously: „core: memory schema and stores“ – `core/memory/`: `MemoryItem` (SPEC §8.2/§17.1: Typen working/episodic/semantic/project/preference/habit/relationship/procedural/visual, Quelle explicit_statement/observation/correction mit Default-Confidence 0.9/0.5/0.95, Retention durable/temporary(expires_at)/session, project_scope, pinned, Versionskette supersedes/superseded_by), `MemoryStore` (Tabelle `memory_items` auf derselben Engine; find/list/search; Suche = Token-Overlap + Cosine über `Embedder`-Interface, `HashingEmbedder` als deterministischer Offline-Fake, gewichtet mit Confidence und Recency; superseded/abgelaufene Items ausgeschlossen; projektbezogene Suche sieht auch globale Items), `MemoryWriter` (einziger Schreiber: Policy-Filter Privacy-Mode/Conversation-Memory/Don't-Learn, `secret` nur mit Owner-Freigabe; Upsert nach subject+predicate+scope: gleiche Aussage ⇒ Reinforcement, stärkere Aussage oder Korrektur ⇒ neue Version supersedet alte, schwächere Aussage ⇒ `memory.conflict`; `correct`, `forget`, `forget_since` („vergiss die letzten 30 Minuten“: neue Items gelöscht, Reinforcements zurückgerollt, Pins bleiben), `pin`, `make_temporary`, `dont_learn`, `purge_expired`, Session-Memory beim Start verworfen). Alle `memory.*`-Events enthalten nur Metadaten, nie den Wert (Test prüft das). CoreRuntime verdrahtet Store+Writer, `/health` zeigt `memory_items`. 11 Tests (142 Core gesamt). Phase-4-Exit „Vergessen löscht reproduzierbar“ und „Confidence/Source sichtbar“ im Store erfüllt; „explizit gespeicherte Fakten werden später korrekt retrieved“ im Store erfüllt, Anbindung an den Agenten folgt mit dem Context Builder.
Previously: „core: subagent orchestration“ – `core/agents/roles.py`: Rollen research, implementation, test, verification, security (SPEC §6.3) mit eigenem Prompt-Zusatz, Risiko-Obergrenze für Tools (research/verification/security nur P0, test bis P2) und Routing-Pfad. Coordinator bietet dem Modell das Tool `agent.delegate {role, goal}` (nur auf Tiefe 0); Sub-Runs sind kontext-isoliert (eigene Historie, gleiche Mission-Korrelation, Events `agent.subrun.started|finished`), laufen bei mehreren Delegationen in einem Schritt parallel (max. 4, `asyncio.gather`), teilen sich den Missions-Budget-Tracker; Ergebnisse gehen in Aufrufreihenfolge als Tool-Messages an den Eltern-Run. Sub-Runs können nicht auf Freigaben warten: ein `awaiting_approval` wird deterministisch abgelehnt (Audit) und an den Coordinator eskaliert, der die Aktion selbst ausführt und regulär pausiert. Pause-Event enthält jetzt auch den Budget-Verbrauch (Resume exakt). 6 Tests (131 Core gesamt).
Previously: „core: agent coordinator“ – `core/agents/`: `AgentRun` (SPEC §17.1), `AgentCoordinator`: ModelRouter wählt Modell/Effort → Provider.complete mit ToolSpecs der Mission-Allowlist → `filter_tool_calls` (Rejects als Event + Tool-Message zurück ans Modell) → jeder erlaubte Vorschlag über `VerifiedExecutor` (Permission → Gateway → Verifier), Ergebnis inkl. Verifikationsstatus als `tool`-Message → bis end_turn, Budget (Schritte/Tool-Calls/Tokens/Kosten/Zeit), Refusal oder Kill Switch. Bei `awaiting_approval` wird der komplette Run-Zustand als Event `agent.run.paused` persistiert; `resume(decision_id)` setzt nach Freigabe fort – auch nach Prozessneustart (Test). Events `agent.run.started|step|paused|resumed|finished|budget_exceeded`, `agent.tool.proposed|rejected`. CoreRuntime: `JARVIS_PROVIDER=claude|mock|none`, `/health` zeigt `providers` und `agent_ready`; `/commands` schickt Nicht-Fast-Path-Kommandos an den Coordinator (BLOCKED nur noch ohne Provider); `/approvals/*/deny` bricht auch Agent-Missionen ab. 11 Tests (125 Core gesamt). Phase-3-Exit: komplexe Mock-Mission ✔, Allowlist eingehalten ✔, Abbruch/Timeout/Retry ✔ (Budget, Kill Switch, Gateway-Retry).
Previously: Commit 010 „ai: claude provider skeleton“ – `core/models/`: `IntelligenceProvider`-Interface (Message, ToolSpec aus Capability-Manifest als striktes JSON-Schema, ToolCallProposal, Usage, ProviderResult; Provider *schlagen* Tool-Calls nur vor), `filter_tool_calls` (Zero-Trust-Allowlist), `ModelRouter` (Fast→lokal/klein, Smart→Sonnet 5, Deep→Opus 5 mit Effort low/medium/xhigh; secret/offline ⇒ nur lokale Modelle; Kostenlimit), `AgentBudget`/`BudgetTracker` (Zeit, Tokens, Kosten, Tool-Calls, Schritte), `MockProvider` (offline, skriptbar), `ClaudeProvider` (Anthropic-SDK lazy, `claude-opus-5` Default, adaptive thinking, `output_config.effort`, strict tools, Prompt-Cache auf System, server-seitige Refusal-Fallbacks, Refusal-Kategorie ausgewertet; Key nie im Request/Log, optionaler Credential-Broker). 10 Tests ohne Netz über Fake-Client (114 Core gesamt). Noch nicht in CoreRuntime/Intent-Router eingehängt (bewusst: Agent Coordinator ist Phase 3).
Previously: Commit 009 „api: websocket event stream“ – `core/runtime.py` (CoreRuntime: ein Prozess, alle Module auf einer DB; `recover()` nach Restart), `core/intents/` (deterministischer Fast-Path-Router: echo/clock/open-URL/stop, sonst `agent`), `core/api/` (FastAPI, Bind 127.0.0.1:7870 via `python -m core`): `/health`, `/events` (Replay), `/ws/events?after_seq` (Replay-then-Live ohne Lücke, streamt nur persistierte Zeilen), `/commands` (Text → Intent → Mission → VerifiedExecutor; Deep-Path-Kommandos werden ehrlich als BLOCKED markiert bis Phase 3), `/missions`, `/approvals/{id}/approve|deny` (setzt die wartende Mission fort bzw. bricht ab), `/kill`, `/resume` (nur starker Proof), `/debug` (statisches Dashboard). 27 Tests (104 Core gesamt). Damit Core-0.1-DoD (SPEC §5.4) bis auf den Claude-Agent-Zweig des Intent Routers erfüllt.
Known limitation (0.1, nur Fast-Path-Kommandos): die Bindung „pending approval → wartendes Kommando“ lebt im Speicher; Agent-Runs sind davon nicht betroffen (persistent über `agent.run.paused`).
Previously: Commit 008 „core: verifier interface“ – `core/verifier/`: `Outcome` achieved/not_achieved/unknown/skipped, `Verification` mit Evidence, `VerifierRegistry`, `VerificationService` (prüft nach jeder Invocation den im Manifest genannten Verifier; unbekannter/kaputter/zu langsamer Verifier ⇒ unknown, nie Erfolg; Events `verification.passed|failed|unknown|skipped` korreliert), `RetryPolicy` (Retry nur bei Tool-Erfolg + not_achieved innerhalb `manifest.retries`; Tool-Fehler/Timeouts werden bereits im Gateway wiederholt, unknown nie blind), `VerifiedExecutor` (invoke → verify → retry; `ok` nur wenn Tool lief UND Ziel verifiziert bzw. nichts zu verifizieren – Law 8), Mock-Verifier `mock.url_recorded`. 10 Tests (76 Core gesamt). Damit erfüllt: Phase-1/2-Exit „Verifier unterscheidet Tool aufgerufen von Ziel erreicht“.
Previously: Commit 007 „core: capability registry“ – `core/capabilities/`: `CapabilityManifest` (SPEC §17.2; Input-Schema, requires, side_effects ⇒ Verifier Pflicht und nie P0), `CapabilityRegistry` mit Health, Mocks `mock.echo` (P0), `mock.clock` (P0), `mock.open_url` (P1, requires device.trusted, nur Aufzeichnung), `ExecutionGateway` als einziger Ausführungspfad: Kill Switch → Manifest/Inputs/Requirements → PermissionEngine (allow → ausführen, ask → `awaiting_approval`, deny → nie) → Grant single-use (`permission.consumed`) → Timeout/Retry → Health → Events `capability.invoked|succeeded|failed|timeout|denied|awaiting_approval|invalid|halted`. Wiederaufnahme nach Freigabe nur mit passendem Grant (gleiche Capability, Korrelation, Argumente). Kill Switch `halt()` stoppt alles (auch P0), `resume()` nur mit starkem Proof von vertrauenswürdigem Gerät. 12 Tests (66 Core gesamt). Damit erfüllt: Phase-1-Exit „Mock-Tool nur nach Permission“, Phase-2-Exit „riskante Aktion wartet auf Approval“ und „Denied nicht umgehbar“.
Previously: Commit 006 „security: permission engine“ – `core/permissions/`: `RiskLevel` P0–P6, `Decision` allow/ask/deny, `PermissionRequest`/`PermissionDecision` (SPEC §17.1 inkl. rule, approval proof, expiration), `Policy` deterministisch (P0–P2 allow, P3 ask+UI-Confirm, P4 ask+stark, P5 nur mit vertrauenswürdigem Gerät + stark, P6 immer deny; verbotene Aktionen; Overrides/TTLs nur verschärfbar, auch relativ zur aktuellen Policy – Law 9), `PermissionEngine`: evaluate → allow (zeitlich begrenzter Grant) / ask (pending mit Ablauf) / deny; approve nur mit ausreichend starkem Proof, Voice nie, P4+ nur von vertrauenswürdigem Gerät; abgelaufene Asks werden zu Deny-Audit-Records; jede Entscheidung als Event `permission.allowed|ask|approved|denied`; Rebuild aus dem Log nach Restart. 17 Tests (54 Core gesamt).
Previously: Commit 005 „core: mission state machine“ – `core/missions/`: `Mission`/`Task` (SPEC §17.1), deterministische Transitionstabellen (SPEC §5.3; jeder Nicht-Terminalzustand ist abbrechbar), `MissionEngine` als einziger Schreiber: validiert → publiziert Event (`mission.<status>`, `mission.task.<state>`, `mission.checkpoint`, korreliert per mission_id) → speichert Snapshot (`missions`-Tabelle, gleiche Engine wie Event-Log). Tasks: Dependencies müssen existieren und abgeschlossen sein, Start nur bei RUNNING-Mission, Retry-Zähler. Restart: Snapshot wird geladen; `rebuild_from_log()` rekonstruiert alle Missionen deterministisch aus dem Event-Log (Test: identisch zum Snapshot). 13 weitere Tests (37 Core gesamt).
Previously: Commit 004 „core: typed event bus“ – `core/events/`: validiertes Event-Envelope (SPEC §5.2, frozen dataclass, JSON-Roundtrip, Korrelationskette via `follow_up`), `SQLEventStore` (SQLAlchemy Core, SQLite/Postgres, append-only mit `seq`-Ordnung, idempotent per event_id, Replay nach Korrelation/Typ-Präfix/seq), `EventBus` (persistiert vor Zustellung, Muster `*`/`prefix.*`/exakt, sync+async Handler, Fehlerisolation, `replay_to` für State-Rebuild nach Restart). 24 Core-Tests unter `tests/core/`, strikte Ruff-Regeln nur für neuen Code.
Previously: Phase 0 – Blueprint als Source of Truth im Repo (PDF, SPEC.md, SECURITY.md, PERFORMANCE.md, ADR-0000..0002, CLAUDE.md), Docker Compose PostgreSQL/pgvector + `.env.example`, CI (format, lint, tests, secret scan, smoke). Exit-Kriterien: Clean clone → `pip install -r requirements.txt` → 33/33 Tests grün (GitHub Actions Run 33957387633 auf Python 3.11, lokal auch auf 3.12 ohne openWakeWord); gitleaks ohne Fund; ADR-0000..0002 vorhanden.
Note: Volle requirements.txt ist unter Linux nur mit Python 3.11 installierbar (openWakeWord → tflite-runtime ohne 3.12-Wheels); Windows/macOS sind nicht betroffen. Core (`core/`) zielt auf 3.12 und bekommt ab Phase 1 einen eigenen schlanken CI-Job.
Active branch: claude/leg-los-lkj4yb (neu aufgesetzt von main nach Merge von PR #3, Merge-Commit 0f8c12e)
Known blockers:
- **Repo-Sichtbarkeit:** `Hahne111/Jarvis` ist aktuell PUBLIC. Blueprint §18/Phase 0 verlangt ein privates Repository (Spec, Memory-Konzepte, spätere Releases). Owner-Entscheidung: Repo auf private stellen oder Blueprint-PDF/SPEC bewusst öffentlich lassen.
- Branch-Schutz für `main` muss der Owner in den GitHub-Repo-Einstellungen setzen (PDF Phase 0, Schritt 1); Workflow: Feature-Branch → PR → CI grün → Merge.
- Blueprint §30: offene Hardware-/Geräte-/Voice-Entscheidungen stehen aus; blockieren Phase 1 nicht.
Open security findings:
- Legacy-Voice-Prototyp (`jarvis/`) führt Tools ohne Permission Engine/Verifier aus (dokumentiert in SECURITY.md §8, ADR-0001). Wird durch Phase 1–2 behoben, nicht durch Patch des Prototyps.
Next exact task: Phase 7, Commit „core: coding workflow for agent roles“ (Schritt 48, SPEC §6.3/§12.2) – `core/agents/roles.py`: Rollen um Werkzeugklassen erweitern (implementation: `workspace.*` voll; test: `workspace.read|list|run` mit Kommando-Einschränkung auf `pytest`/`python -m pytest`; verification: nur `workspace.read|diff|list`), Coordinator-Systemprompt um den Coding-Workflow („schreibe → führe Tests aus → verifiziere; Mission gilt erst nach grünem Run als erledigt“), `AgentCoordinator` markiert Mission-Erfolg nur, wenn der letzte `workspace.run` mit Exit 0 verifiziert wurde, sobald ein Workspace benutzt wurde (sonst Outcome `not_verified`); Artifact-Registry-Minimal: Event `artifact.created` (Pfad, SHA-256, Größe) beim Abschluss für alle Dateien des Workspaces (Schritt 49, Basis für Preview/Download). End-to-End-Test mit MockProvider: Datei + Test schreiben, `workspace.run pytest` (Approval per Test), Mission COMPLETED erst nach grünem Lauf; roter Lauf ⇒ FAILED mit Terminal-Events.
Old plan for reference: Phase 7, Commit „apps/desktop: coding mode panels“ (Schritte 43/47 im Web-HUD) – HUD-Modus „Coding“ (SPEC §3.2): Projektbaum aus `workspace.list` der aktiven Mission, Dateiansicht aus `workspace.read` (zunächst Monaco per CDN mit Fallback auf `<pre>`; Tauri-Version bündelt Monaco lokal), Diff-Panel aus `workspace.file.changed`, Terminal-Panel aus `workspace.run.output`-Chunks mit Fehler-Hervorhebung, Preview-Panel als `<iframe>` auf einen statischen Preview-Endpunkt `GET /workspace/{mission}/preview/<path>` (nur HTML/CSS/JS-Dateien des Workspaces, Sandbox-Attribut, keine Core-Cookies). Danach 48: Coordinator-Rollen `implementation`/`test`/`verification` erhalten `workspace.*` in ihrer Allowlist (test: `workspace.run` mit pytest; verification: nur read/diff) und der Agent-Prompt bekommt den Coding-Workflow (schreiben → testen → verifizieren) – End-to-End-Test mit MockProvider: Datei schreiben, pytest laufen lassen, Mission erst nach grünem Run COMPLETED.
Old plan for reference: PHASE 7 Live Coding Mode, Commit „core: workspace and sandboxed project runner“ (SPEC §12, Phase-7-Schritte 44–46) – `adapters/workspace/`: Capability-Familie `workspace.*` für einen **isolierten Projektordner** (`JARVIS_WORKSPACE_ROOT`, Default `jarvis/data/workspaces/<mission_id>`): `workspace.list` (P0), `workspace.read` (P0), `workspace.write` (P2, reversibel über Versionskopie, Verifier `workspace.file_matches`), `workspace.diff` (P0), `workspace.run` (P3 ⇒ Approval; Kommando-Allowlist z. B. `python`, `pytest`, `node`, Timeout, CPU/Zeit-Budget, kein Netz-Flag, Ausgabe als Event-Stream `workspace.run.output` gechunkt), Verifier `workspace.run_exit_code`; Pfad-Sandbox (kein `..`, keine Symlinks nach außen), Diff-Events `workspace.file.changed` für das HUD (Editor/Diff-Panel folgt). Tests: Pfad-Escape unmöglich, Write verifiziert, Run mit Timeout/Allowlist, Ausgabe-Chunks im Event-Log. Danach 43/47: Monaco-Editor + Preview-Panel im Web-HUD, 48 Writer/Test/Verifier-Rollen an `workspace.*` binden.
Old plan for reference: Phase 6, Milestone „apps/desktop: minimal HUD shell“ (Schritte 36/41/42) – Entscheidung per ADR-0003 festhalten: Web-first Shell (`apps/desktop/web/`, Vanilla-TS oder React ohne Build-Zwang, vom Core unter `/hud` ausgeliefert) als Vorstufe der Tauri-Shell, weil die Tauri/Rust-Toolchain in der CI-Umgebung fehlt; Inhalt: Central-Core-Anzeige aus `presence.changed`, Event Rail (WS `after_seq`, Filter nach Bereich laut HUD_EVENTS.md), Approval-Panel (Proof-Auswahl ui_confirm/passkey), Mission-Liste, Memory-Panel, Kill Switch; Amber/Cyan-Designsprache (SPEC §3), 60-FPS-Regel: keine synchronen Requests im Render-Pfad, Event-Batching pro Frame. Tests: statische Auslieferung + Smoke über TestClient; visuelle Prüfung manuell. Danach Tauri-Wrapper (`apps/desktop/src-tauri`) als eigener Milestone, wenn die Toolchain lokal verfügbar ist.
Old plan for reference: Phase 6, Commit „core: presence states and HUD event contract“ (Schritt 37 + Vorbereitung 36/42) – `core/presence.py`: `PresenceService` mit Zuständen idle/listening/thinking/speaking/working/awaiting_approval/halted, abgeleitet deterministisch aus Bus-Events (voice.*, agent.run.*, permission.ask, gateway.halted) und als `presence.changed` (device_id, state, since, active_mission) publiziert; `GET /presence` + Feld in `/health`; `docs/HUD_EVENTS.md`: der Event-Vertrag für die Tauri/React-Shell (welche Event-Typen die HUD-Modi Core/Mission/Approval/Memory speisen, Reihenfolge, Replay via `/ws/events?after_seq`). Danach Milestone „apps/desktop: Tauri + React Shell“ (Schritt 36; eigener Toolchain-Check: Node 20+, pnpm, Rust – nur wenn lokal vorhanden, sonst Web-Preview der Shell im Browser gegen `127.0.0.1:7870`).
Old plan for reference: PHASE 6 Desktop Agent + Minimal HUD, Commit „adapters: desktop capabilities behind the core“ (SPEC §10.2, Phase-6-Schritte 38–40) – `adapters/desktop/`: Capability-Manifeste für `computer.open_app` (P1, requires device.trusted, Verifier `computer.process_running`), `computer.list_windows` (P0), `computer.focus_window` (P2, Verifier `computer.window_focused`), `computer.screenshot` (P0, Sensitivity private, Datei nur lokal), `computer.type_text`/`press_key` (P3 ⇒ Approval), `system.volume` (P1), `system.lock` (P2) – Handler als dünne Wrapper über die unveränderten Prototyp-Funktionen (`jarvis/tools/app_control.py`, `desktop.py`, `system.py`), Registrierung in CoreRuntime per `JARVIS_DESKTOP=1` (Default aus, damit CI ohne Display bleibt), Fake-Backend für Tests; Exit „App öffnen und verifizieren“ mit Fake-Backend, Approval-Pfad für Tastatureingaben. Danach Presence States (Schritt 37) als Events `presence.*` und das minimale HUD (Tauri/React) als eigener Milestone.
Before Phase 6 (owner, local): echten Voice-Lauf machen: `pip install -r requirements.txt`, `python -m voice` (Mikrofon/Lautsprecher), Wake-Ack/Barge-in-Latenzen aus `/events?type_prefix=telemetry` hier eintragen.
Old plan for reference: PHASE 5 Voice 0.1, Commit „voice: pipeline interfaces and core bridge“ (SPEC §9, Phase-5-Schritte 29–35) – `voice/` gemäß SPEC §20 anlegen (Delta zu ADR-0001: Prototyp-Module werden *kopiert/adaptiert*, nicht verändert): `voice/routing/`: `VoiceSession`-Zustandsautomat (idle → wake_ack → listening → thinking → speaking → follow-up) mit Events `voice.wake|listening|transcript|speaking|barge_in|idle`, Latenz-Telemetrie (Wake-Ack, erstes Audio, Stop-Zeit als Events `telemetry.latency`); Interfaces `WakeWordDetector`, `SpeechToText` (streaming), `TextToSpeech` (phrase-level streaming, `stop()` < 150 ms), Fake-Implementierungen für Tests; Bridge: Transkript → `POST /commands` bzw. direkt `IntentRouter` (Fast Path lokal ohne Claude) → Antworttext → TTS; „Jarvis, stopp“ ⇒ `gateway.halt` + TTS stop (Barge-in). Adapter an den Prototyp (`jarvis/wake.py`, `jarvis/stt.py`, `jarvis/tts.py`) als dünne Wrapper in `voice/adapters/prototype.py`, nur importiert, unverändert. Exit: Wake-Ack-Event sofort, Barge-in stoppt Fake-TTS < 150 ms (Test), simpler lokaler Befehl läuft ohne Provider.
Old plan for reference: Phase 4, Commit „core: context builder and memory API“ (Schritte 26–28) – `core/memory/context.py`: `ContextBuilder.build(goal, project_scope?, sensitivity, max_items=8, max_chars=1500)` wählt per `MemoryStore.search` nur relevante, nicht-geheime Items (secret nie in einen Cloud-Prompt; bei lokalem Modell erlaubt) und rendert sie kompakt mit Quelle+Confidence in den Agent-Systemprompt („Known about the owner/project“); Coordinator ruft ihn pro Run auf, Event `memory.context_used` mit den memory_ids (Nachvollziehbarkeit, SPEC §8.4). Korrektur-Loop: Capability `memory.remember` (P1, kein Verifier-Zwang da kein externer Seiteneffekt – ADR-Notiz) und `memory.correct`/`memory.forget` (P3, Approval) als Capabilities über Registry/Gateway, damit der Agent Fakten explizit speichern/korrigieren kann, wenn der Owner es sagt. API „What JARVIS Knows“: `GET /memory?type=&project=&q=`, `POST /memory/{id}/correct|forget|pin|unpin|temporary`, `POST /memory/forget_since {minutes}`, `POST /memory/policy` (nur restriktiver ohne Owner-Proof? – Privacy Mode an/aus ist Owner-Aktion, per UI-Confirm), Debug-Dashboard-Panel „What JARVIS Knows“. Tests: Context enthält nur relevante Items und nie secret bei Cloud-Modell; Korrektur über API ändert Antwort des Agenten (MockProvider sieht neuen Kontext).
Offen vor Phase 4 (owner): ein manueller Lauf mit `JARVIS_PROVIDER=claude` gegen die echte API (Key lokal setzen, `python -m core`, im Debug-Dashboard „write me a haiku about the desk lamp“ eingeben) – Ergebnis hier in STATUS.md eintragen.
Known limitation (Phase 3): Ein manueller Lauf gegen die echte Claude-API wurde in dieser Umgebung nicht durchgeführt (kein Key); der Adapter ist nur über den Fake-Client getestet. Offene Integration: MissionEngine setzt bei `awaiting_approval` WAITING_FOR_APPROVAL (Agent Coordinator, Phase 3).
Known limitation (0.1): Event-Log-Eintrag und Mission-Snapshot sind zwei Transaktionen; das Log ist Source of Truth, ein veralteter Snapshot wird per `rebuild_from_log()` repariert. Atomare Schreibweise (gemeinsame Transaktion) ist für Commit 007/Execution Gateway vorgesehen.
Last benchmark: n/a (Telemetrie ab Phase 5)
Last backup/restore drill: n/a (ab Phase 12)
