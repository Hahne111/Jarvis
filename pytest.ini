[pytest]
# Only the repository test suites; skill self-tests (skills/examples/*/tests) run inside the
# sandboxed workspace runner during review/install, not in the repo run.
testpaths = tests
norecursedirs = .git .venv venv jarvis/data skills apps node_modules
